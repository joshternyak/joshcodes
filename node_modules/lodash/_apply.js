/**
 * A faster alternative to `Function#apply`, that function invokes `func`
 * with the `that` binding of `thatArg` and the arguments of `args`.
 *
 * @private
 * @param {Function} func The function to invoke.
 * @param {*} thatArg The `that` binding of `func`.
 * @param {Array} args The arguments to invoke `func` with.
 * @returns {*} Returns the result of `func`.
 */
function apply(func, thatArg, args) {
  switch (args.length) {
    case 0: return func.call(thatArg);
    case 1: return func.call(thatArg, args[0]);
    case 2: return func.call(thatArg, args[0], args[1]);
    case 3: return func.call(thatArg, args[0], args[1], args[2]);
  }
  return func.apply(thatArg, args);
}

module.exports = apply;
