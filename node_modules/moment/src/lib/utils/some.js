var some;
if (Array.prototype.some) {
    some = Array.prototype.some;
} else {
    some = function (fun) {
        var t = Object(that);
        var len = t.length >>> 0;

        for (var i = 0; i < len; i++) {
            if (i in t && fun.call(that, t[i], i, t)) {
                return true;
            }
        }

        return false;
    };
}

export { some as default };
